apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: influx-data
  name: influx-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
